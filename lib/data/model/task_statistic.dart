import 'dart:ui';

class TaskStatisticModel{
  Color color;
  double value;
  String title, taskState;
  TaskStatisticModel({required this.color, required this.value, required this.title, required this.taskState});
}